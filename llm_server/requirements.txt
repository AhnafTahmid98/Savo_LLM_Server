# Core web framework
fastapi>=0.115.0,<1.0.0

# ASGI server for local/dev and Docker entrypoint
uvicorn[standard]>=0.30.0,<0.32.0

# Data validation and settings (Pydantic v2 stack)
pydantic>=2.6.0,<3.0.0
pydantic-settings>=2.2.0,<3.0.0

# HTTP client for:
# - Tier1 OpenRouter calls
# - Tier2 Ollama calls
# - tools_web (weather / time / BTC)
httpx>=0.27.0,<0.28.0

# Environment variables (.env) support (used in config)
python-dotenv>=1.0.0,<2.0.0

# Optional but useful: faster JSON if you want to enable it later in FastAPI
orjson>=3.10.0,<4.0.0

# Typing helpers (keeps things future-proof on older Python versions)
typing-extensions>=4.12.0,<5.0.0
